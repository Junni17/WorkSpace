package montyHallParadox;

public class GameApp {

	public static void main(String[] args) {

		Game game = new Game();
		game.placeCar();
		game.firstPick();

		System.out.println(game.getCar());
		System.out.println(game.firstPick());

		// int doors = 3;

		// for (int i = 0; i < 1000; i++) {
		//
		// int car_door = (int) (Math.random() * doors + 1);
		// int door_pick = (int) (Math.random() * doors + 1);
		// int goat_door1 = (int) (Math.random() * doors + 1);
		// int goat_door2 = (int) (Math.random() * doors + 1);
		//
		// // Deciding where the car and goats are placed:
		//
		// while (car_door == goat_door1 || car_door == goat_door2 || goat_door2
		// == goat_door1) {
		//
		// if (car_door == goat_door1) {
		// goat_door1 = (int) (Math.random() * doors + 1);
		// } else if (car_door == goat_door2 || goat_door1 == goat_door2) {
		// goat_door2 = (int) (Math.random() * doors + 1);
		// }
		// }
		//
		// // System.out.println("Car door: " + car_door);
		// // System.out.println("Goat doors: " + goat_door1 + " and " +
		// // goat_door2);
		//
		// }
	}

}
